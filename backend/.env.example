APP_NAME=SightAhead API
ENVIRONMENT=local
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# OpenAI (орчуулга — үндсэн)
OPENAI_API_KEY=
OPENAI_TRANSLATION_MODEL=gpt-4o-mini
OPENAI_ASSISTANT_MODEL=gpt-4o-mini
TRANSLATION_PROVIDER=openai

# Azure TTS
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=southeastasia
TTS_PROVIDER=azure

# Firebase
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json
FIREBASE_CREDENTIALS_JSON=

# Gemini (орчуулга fallback)
GEMINI_API_KEY=

# Транскрипт (RapidAPI 'youtube-transcriptor') — YouTube IP-block тойрно
RAPID_API_HOST=youtube-transcriptor.p.rapidapi.com
RAPID_API_KEY=

# GPU voice-over (Modal F5 app — gpu/f5_modal.py deploy хий)
MODAL_F5_APP=sightahead-f5
MODAL_F5_CLASS=F5
# Backend-ээс Modal-д нэвтрэх: `modal token new` (~/.modal.toml) эсвэл эдгээр:
# ⚠️ Жинхэнэ утгыг энд БҮҮ бич (.env.example git-д ордог). Зөвхөн .env-д бич.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# Cost guardrails — GPU зарцуулахаас өмнө хэт урт оролтыг таслана
MAX_DUB_SEGMENTS=1500
MAX_TRANSCRIPT_CHARS=60000

# Audio storage — Cloudflare R2 (S3-нийцтэй). Dashboard → R2 → bucket + API token
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# Bucket-ийн public URL (r2.dev dev URL эсвэл custom domain), ж: https://pub-xxxx.r2.dev
R2_PUBLIC_BASE_URL=

# Firebase deploy дээр (Railway/Render) — file биш JSON/base64 ашигла
FIREBASE_CREDENTIALS_JSON_BASE64=

# Логийн түвшин (DEBUG = per-segment лог)
LOG_LEVEL=INFO

# Локал тест
ENABLE_LOCAL_PROCESSING=true
