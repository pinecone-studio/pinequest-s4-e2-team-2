fastapi
uvicorn[standard]
python-dotenv
pydantic

# Auth / Database (Firebase Auth + Firestore)
firebase-admin

# Caption
youtube-transcript-api

# Translation + Summary
openai
google-generativeai
httpx
mutagen

# GPU dub: call the Modal F5 app (gpu/f5_modal.py) via the SDK
modal

# Audio storage: Cloudflare R2 (S3-compatible) via boto3
boto3

# Audio (duration only — pydub/azure SDK not used; TTS calls Azure REST via httpx)
mutagen

# FastAPI extras
python-multipart
